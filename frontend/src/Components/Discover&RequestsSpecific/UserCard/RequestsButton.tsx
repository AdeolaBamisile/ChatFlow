import type { RequestUser } from "../../../types";

interface RequestsButtonProps {
  user: RequestUser;
}

const RequestsButton = ({ user }: RequestsButtonProps) => {};

export default RequestsButton;
